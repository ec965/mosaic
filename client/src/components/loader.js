import React from 'react';
import Loader from 'react-loader-spinner';
import { COLORS } from '../config/colors';

const MyLoader = () => <Loader type="Oval" color={COLORS.base0D} height={80} width={80}/>
export default MyLoader