const auth = require("./auth");
const app = require("./app");
const project = require("./ project");
const validate = require("./validate");
const password = require("./password");

module.exports = { auth, app, project, password, validate };
